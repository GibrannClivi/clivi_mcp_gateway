import { SubscriptionEvent } from '../models/SubscriptionEvent';

export class HubSpotMCPContext {
  async receive(event: SubscriptionEvent) {
    // TODO: Implement logic to update HubSpot CRM based on SubscriptionEvent
    // Map SubscriptionEvent to CRMUserContext and call HubSpot API
  }
} 